write HTML:5 to start the html coding 

To hide something from the main page without deleting it, select, and push / 

Push command s to save something 

 Use a to create <a href="type theme of the link here">place the name you want to display here</a> links. 

 <a href="https://instagram.com/barefeet_bluejeans">Bethany's instagram</a> link to instagram,
  other links should be written out the same way, with the name of the file you wish to access.
  
   Write <a href="https://www.instagram.com/barefeet_bluejeans"target="_blank">instagram profile</a> 
   The important part of this is that target="_blank" makes the tab open a new page
   
    .contact-hours-wrapper turned into "<div class="contact-hours-wrapper"></div>"
    
     .name-of-file is correct css forum
     
      # before id in css
      
       It is recommended to always use class instead of id
       
        To find the closing div, push the div, and it will highlight both the opening and closing div 
        
        Padding starts on the top and goes clockwise If you have the same padding for the top and botom,
         you can place just one measurement for the top and bottom, and another for the right and left
        exp,
        20px and 60px 
flex box and grid are better used for different things
flex box is better used when you don't know the number of variables
grid is better when you know

type ../ to go back a file in css

background-attachment: fixed; means that the photo wont move
background-position: center; center's the photo in the middle
background-repeat: no-repeat; by default images repeat, but will only show on a large screen
no-repeat will make sure that someone will not be seeing more than one images
background-size: cover; maintains the ratio, performs a crop of the image, if it is centered
it will take out the center, if it did not crop how you wanted it to, edit the settings

inset box shadow is what you call the shadow in a flex box

if you ever want to work with trancperacy, use rgba for selecting colors

to make a box shadow type box-shadow,  leftpx toppx bluringpx right or all aroundpx

css box shadow generator is the online file to create shadow bow info

inset or outline can be used in the shadow box

html entities list of all the entitties number or name can be used

to stack the divs ontop of eachother use "flex-direction: column;"

text transform for the upercase function

unsplash is a place to go to get images for your website

when creating a list, use ul for unordered list to have the beautiful dots on the side (bullet points) or use ol for ordered list 